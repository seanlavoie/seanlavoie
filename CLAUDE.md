# CLAUDE.md — personal-site

## After every commit

Always push to remote immediately after committing:

```bash
git push origin main
```

Do not consider a task done until the commit is on `origin/main`.
